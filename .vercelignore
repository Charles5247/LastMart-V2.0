# Runtime files not needed on Vercel (Next.js frontend only)
# The Express backend is deployed separately on Railway/Render/VPS
# next.config.js rewrites /api/* → BACKEND_API_URL at runtime — no backend code needed here

mobile/
*.db
*.db-shm
*.db-wal
lastmart_seed.sql
ecosystem.config.cjs
